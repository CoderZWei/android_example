<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minHeight="180dp"
    android:minWidth="300dp"
    android:previewImage="@mipmap/widget_icon"
    android:initialLayout="@layout/widget_layout"
    android:updatePeriodMillis="50000"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen|keyguard">
<!--
    minHeight、minWidth表示widget的最小高度和宽度
    previewImage表示小部件的图标
    initalLayout表示wedget的布局
    resizeMode指定widget的可调整尺寸的规则，horizontal表示可以水平拉伸
    widgetCategory表示widget可显示的地方，home_screen或lock screen
-->
</appwidget-provider>